extends Area2D


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(_delta):
	pass

"""func body_entered(_Body_hitbox):
	if Input.is_action_just_pressed("basic attack") and XiJinPing.honey == false:
		GlobalHealth.p1health -= 5
	elif Input.is_action_just_pressed("Kick_attack") and XiJinPing.honey == false:
		GlobalHealth.p1health -= 10
	elif Input.is_action_just_pressed("basic attack") and XiJinPing.honey == true:
		GlobalHealth.p1health -= 7
	elif Input.is_action_just_pressed("Kick_attack") and XiJinPing.honey == true:
		GlobalHealth.p1health -= 15"""
